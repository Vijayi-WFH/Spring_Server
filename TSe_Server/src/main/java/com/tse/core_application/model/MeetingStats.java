package com.tse.core_application.model;

public enum MeetingStats {

    MEETING_SCHEDULED,   // set when meeting is created
    MEETING_STARTED,
    MEETING_DELAYED,
    MEETING_ENDED,
    MEETING_OVER_RUN,
    MEETING_ENDED_WITH_OVER_RUN,
    MEETING_COMPLETED,
    MEETING_COMPLETED_WITH_OVER_RUN
}
